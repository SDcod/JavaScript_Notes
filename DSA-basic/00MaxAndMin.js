let arr = [1, 2, 34, 12, 59, 4];

// let f = (a) => {
//   let maxNum = (minNum = a[0]);

//   if (a.length > 0) { //handling edge case
//     for (let i = 0; i < a.length; i++) {
//       if (a[i] > maxNum) {
//         maxNum = a[i];
//       }
//       if (a[i] < minNum) {
//         minNum = a[i];
//       }
//     }
//     return { maxNum, minNum };
//   }
//   return null;
// };

// console.log(f(arr));

//********************************************using js methods

// console.log("Max " + Math.max(...arr));
// console.log("Min " + Math.min(...arr));
